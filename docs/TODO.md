# TODO

## CI/CD
- [ ] `buf breaking --against '.git#branch=main'` を CI に組み込み、Proto の破壊的変更を自動検知する
- [ ] `buf lint` を CI に組み込み、Proto の命名規約・構造ルールを自動チェックする
- [ ] `buf format` を CI に組み込み、Proto のフォーマットを統一する
